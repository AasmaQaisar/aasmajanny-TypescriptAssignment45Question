let users = ['Bunny', 'Janny', 'Candy'];

if (users.length === 0) {
  console.log('We need to find some users!');
} else {
  // Loop through the users array and print each username
  for (let i = 0; i < users.length; i++) {
    console.log(users[i]);
  }
}

// Remove all of the usernames from the array
users = [];

// Check if the array is now empty
if (users.length === 0) {
  console.log('We need to find some users!');
}

// Modified Exercise 28 with if test

// Define a list of users
users = []




