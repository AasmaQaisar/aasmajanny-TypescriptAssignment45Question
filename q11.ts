// Define an array of names
const names = ['Aasma', 'Janny', 'Fatima', 'Ayesha'];
// Loop through the array and print each name
for (let i = 0; i < names.length; i++) {
  console.log(names[i]); // Print each name one at a time
}
