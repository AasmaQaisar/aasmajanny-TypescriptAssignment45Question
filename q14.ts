// Define the guests list
const guestList = ['Fiza', 'Zaynab', 'Mirha','Miral','Mehrma'];

// Loop through the list and print an invitation message for each guest
guestList.forEach(guest => {
  console.log(`Dear ${guest}, you are cordially invited to dinner at my place on Saturday at 7 PM. `);
});
