// Define an array of names
const names = ['Aasma', 'Janny', 'Fatima', 'Ayesha'];
for (let i = 0; i < names.length; i++) {
    const message = `Hello ${names[i]}, how are you doing today?`;
    console.log(message);
  }